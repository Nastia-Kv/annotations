package arithmetic;

public class Addition {

    public double add(double a, double b) {
        double c = a + b;
        System.out.println("a (" + a + ") + b (" + b + ") = " + c);
        return c;
    }
}
