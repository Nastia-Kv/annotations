package arithmetic;

public class Subtraction {

    public double subtract(double a, double b) {
        double c = a - b;
        System.out.println("a (" + a + ") - b (" + b + ") = " + c);
        return c;
    }
}
