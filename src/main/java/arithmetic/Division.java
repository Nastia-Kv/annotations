package arithmetic;

public class Division {

    public double divide(double a, double b) {
        double c = a / b;
        System.out.println("a (" + a + ") / b (" + b + ") = " + c);
        return c;
    }
}
